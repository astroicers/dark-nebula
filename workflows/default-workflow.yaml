apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  name: default-workflow
spec:
  entrypoint: whois
  templates:
  - name: whois
    container:
      image: astroicers/whois
      args: ["google.com"]
